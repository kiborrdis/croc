import { CrocGameState } from './CrocGameState';
import { CrocGameContext } from '../CrocGameContext';

export class WaitState extends CrocGameState {
  public enter(context: CrocGameContext) {
    super.enter(context);
  }
}
